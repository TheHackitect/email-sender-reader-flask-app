## Flask App That interacts with your email inbox
- Obtain Your credentials from google developer console
- there must be a `credentials.json` and `token.json`

-Reads your `email inbox`, `Send Emails`, `Send Bulk emails`
